scenario: S02
name: Retry Storm Amplification
patterns: [P06, P03]
composition: C02
expected_steps: 7
red_herrings:
  - "downstream service ML model too slow"
  - "Postgres connection pool exhaustion"
  - "mobile app retry logic too aggressive"
root_cause: "Hidden retry defaults enabled by dependency upgrade multiply requests exponentially across layers"
fix_keywords:
  - "circuit breaker"
  - "retry disabled"
  - "NEVER_RETRY"
  - "idempotency"
  - "max_retries=0"
  - "no retry"
  - "retry budget"
